extend type Query {
    logout: ResponseDto
}

extend type Mutation {
    login(loginRequest: LoginRequest!): AuthenticationResponse
    refreshToken(userFingerprintHash: String!): AuthenticationResponse
}

type AuthenticationResponse {
    accessToken: String
}

type ResponseDto {
    code: Int
    success: Boolean
    message: String
}

input LoginRequest {
    username: String
    password: String
}
